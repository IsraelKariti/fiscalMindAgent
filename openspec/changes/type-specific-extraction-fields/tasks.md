## 1. Catalog declaration

- [x] 1.1 In `src/agents/declarationOfCapital/catalog.ts`, add `FieldKind`, `ExtractionField`, `fields?` and `fieldsAnyOf?` on `CapitalDocumentType`, and `periodCoversValuationDate?` on `VerificationChecks` (design decision 1). Verify: `npm run typecheck` passes with no catalog entry changed yet.
- [x] 1.2 Declare the first set of fields (spec "First set of typed fields"): `SAVINGS_FIELDS` shared by `pension_provident`, `study_fund` and `life_insurance_savings` with `fieldsAnyOf: ['closing_balance', 'total_deposits']`; `bank_balance`; `securities_portfolio`; `mortgage_balance`; `vehicle` with `fieldsAnyOf: ['license_plate', 'purchase_cost']` and `license_plate` pattern `^\d{7,8}$`; `contents_insurance` with `checks.periodCoversValuationDate: { from: 'period_from', to: 'period_to' }`. Each field's `promptHe` names the trap the hint already describes (the contents chapter, not the building sum; the tax-year NAV column, not the comparison column; the 31.12 row of the tax year, not another year). Verify: `tests/capitalCatalog.test.ts` — every field key differs from the base keys and from its siblings, every field has a non-empty `labelHe` and `promptHe`, every `fieldsAnyOf` key and every `periodCoversValuationDate` key is a declared field of that type (dates for the period), and the nine untyped types declare no `fields`.

## 2. Schema, prompt and checks

- [x] 2.1 In `verifyChecks.ts`, add `extractionSchemaFor(type)`, `extractionJsonSchemaFor(type)`, `ExtractedAnswer`, `typeFieldValue(answer, field)` and `typeFieldsPromptBlock(type)` (design decisions 2 and 3); keep `ExtractionSchema`, `extractionJsonSchema` and `ExtractedFields` exported unchanged. Verify: a unit test in `tests/verifyChecks.test.ts` shows `extractionJsonSchemaFor(undefined)` deep-equals `extractionJsonSchema`, `extractionJsonSchemaFor(vehicle)` lists the five vehicle keys as nullable properties, `typeFieldValue` turns `""` into null and keeps 0, and the prompt block has one line per field plus the "at least one" line.
- [x] 2.2 In `extractionCall.ts`, make `checksFor(doc)` also return the type's `fields` and `fieldsAnyOf` (or add `fieldsFor(doc)`), append the prompt block to `{{type_context}}`, and attach `extractionJsonSchemaFor(type)` as `responseJsonSchema`. Verify: `tests/verifyChecks.test.ts` or a new `tests/extractionCall.test.ts` — the request for a prior-declaration row equals today's request text and schema; the request for a vehicle row carries the five instruction lines and the extended schema.
- [x] 2.3 In `runChecks`, accept `ExtractedAnswer`, add `fields`/`fieldsAnyOf` to `CheckContext`, and add the `type_fields` and `period_covers_valuation_date` checks after `amounts` (design decision 4, notes in Hebrew). Verify: `tests/verifyChecks.test.ts` — required field null fails with the label in the note; `fieldsAnyOf` satisfied by `total_deposits: 0` passes; malformed date, year 1900, non-finite number and plate "12-345-67" each fail with the right note; period 2025-01-01 to 2025-11-30 fails `period_covers_valuation_date` for tax year 2025 with observed "2025-01-01 – 2025-11-30" and expected "2025-12-31"; period 2025-03-01 to 2026-02-28 passes; a type with no fields yields neither check; observed text is capped at six pairs.
- [x] 2.4 In `verifyDocument.ts`, parse with `extractionSchemaFor(catalogType)`, pass the fields into `runChecks`, and add `fields: [{ key, label, value }]` to the `verify_extraction` audit detail when the type declares fields (design decision 5). Verify: `npm run typecheck` passes and `npm test` is green.
- [x] 2.5 Update `scripts/verifyExtractionSample.ts` to build its request through `buildExtractionCall` and parse through `extractionSchemaFor`, so the sample run uses the typed schema. Verify: `npx tsx scripts/verifyExtractionSample.ts evals/files/vehicle_licence_2025.pdf vehicle` prints the plate, maker, model and year and the `type_fields` check.

## 3. Trace and labels

- [x] 3.1 In `web/src/components/stepSummary.ts`, add a `verify_extraction` mapping that renders `fields` as one "label: value" item per entry under the key `extracted_fields` (null → "לא נמצא") and marks `fields` consumed; add the Hebrew label for `extracted_fields` and the check labels `type_fields` ("השדות הייעודיים לסוג המסמך נקראו ותקינים") and `period_covers_valuation_date` ("תקופת הפוליסה כוללת את המועד הקובע") in `web/src/i18n.tsx`. Verify: `tests/stepSummary.test.ts` — a detail with three fields renders three items and no generic `fields` row; `cd web && npx tsc --noEmit` passes.

## 4. Evals

- [x] 4.1 In `evals/stages.ts`, extend `VerifyDocumentCase.expected` with `fields` and judge each listed key (design decision 6); pass the type's fields into the harness's `runChecks` call. Verify: `npm run typecheck` (root and `evals/tsconfig.json`) passes.
- [x] 4.2 Add `fields` expectations to the existing cases whose sample values are known (`leumi_balance_2025.pdf` bank fields, `altshuler_study_fund_2025.pdf`, `harel_study_fund_2025.pdf`, `menora_pension_2025.pdf`, `meitav_activity_2025.pdf`) and a vehicle case on `vehicle_licence_2025.pdf`, reading the expected values from the files. Verify: `/run-evals extract_document` — the new checks pass on the current model, or the case notes record why a value is left unjudged.
- [x] 4.3 With the `add-eval-case` skill, add a synthetic contents-insurance PDF (combined home policy: building sum, contents sum, liability limits, premium, period 2025-03-01 to 2026-02-28) with a passing case, and a second case on a copy whose period ends 2025-11-30 expecting `verdict.failed_keys: ["period_covers_valuation_date"]`. Verify: `/run-evals extract_document` passes both and the report compares clean against the last run; commit the result run.

## 5. Docs

- [x] 5.1 Update `docs/agents.md`: the verification paragraph gains the field declaration, the two new checks, the flat-answer rule and the first set of typed types. Verify: the text agrees with the two delta specs.
- [x] 5.2 Notion, per `STYLE.md`: on "4. Extraction for verification" add a "Type fields" section with 17 cards (two per row) linking to new child pages, one per catalog type, each with "What the declaration needs", "Accepted forms", "Common checks that apply", "Extra fields" (one card per field, or the sentence "This type has no extra fields") and "Checks that run"; add the two new checks to the `verify_extraction` gate page. Verify: the parent page lists 17 child pages, every child page opens from its card, and the field lists match `catalog.ts` key for key.

## 6. Live check

- [ ] 6.1 With the local stack running, verify a vehicle licence and a study fund certificate on a test client (mock inbound path) and confirm in the step modal that the `verify_extraction` step lists the typed values by label and the `type_fields` check; record the client id in the change for cleanup. Verify: the step modal and the documents list show the expected result.
